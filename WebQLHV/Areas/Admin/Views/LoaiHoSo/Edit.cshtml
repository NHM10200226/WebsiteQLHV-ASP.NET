@model WebQLHV.Models.LoaiHoSo
@{
    ViewBag.Title = "Edit";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

@section naviheader{
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a href="/admin/home" class="nav-link">Trang chủ</a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a href="/admin/LoaiHoSo" class="nav-link">Danh sách</a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a href="#" class="nav-link">Sửa hợp đồng</a>
        </li>
    </ul>
}

<!-- Content Header (Page header) -->
<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1>Sửa loại hợp đồng</h1>
            </div>
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="#">Loại hợp đồng</a></li>
                    <li class="breadcrumb-item active">Sửa</li>
                </ol>
            </div>
        </div>
    </div><!-- /.container-fluid -->
</section>

<!-- Main content -->
<section class="content">

    <!-- Default box -->
    <div class="card">
        <div class="card-header">
            <h3 class="card-title">Nhập thông tin hợp đồng</h3>
        </div>
        <div class="card-body">
            <div class="bs-stepper-content">
                <!-- your steps content here -->
                <div id="logins-part" class="content" role="tabpanel" aria-labelledby="logins-part-trigger">
                    @Html.HiddenFor(m => m.ID)
                    <div class="form-group">
                        <label for="exampleInputName">Tên hợp đồng</label>
                        @Html.TextBoxFor(m => m.NAME, new { @class = "form-control", @placeholder = "NAME" })
                    </div>
                    <div class="form-group">
                        <label for="exampleInputName">Tên trung tâm</label>
                        @Html.DropDownList("TRUNGTAM", new SelectList(ViewData["LITTT"] as List<SelectListItem>, "Value", "Text"),
                                          new { @class = "form-control select2 select2-blue ", data_placeholder = "Chọn..." })
                    </div>
                </div>
                <div id="information-part" class="content" role="tabpanel" aria-labelledby="information-part-trigger">
                    <a href="@Url.Action("index", new { area = "admin", controller = "LoaiHoSo" })" onclick="onShow();" class="btn btn-danger">Hủy</a>
                    <button type="submit" onclick="editLoaiHoSo()" class="btn btn-primary">Submit</button>
                </div>
            </div>
        </div>
        <!-- /.card-body -->
        <div class="card-footer">
            Footer
        </div>
        <!-- /.card-footer-->
    </div>
    <!-- /.card -->

</section>
<!-- /.content -->
<script src="~/ajax/Admin/ajaxLoaiHoSo.js"></script>


